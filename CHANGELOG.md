# Changelog

## Unreleased

- Add a Dismiss button and a `/queue` slash command so the review backlog can drain: Dismiss is the missing no-op exit (records `action_taken='dismissed'`, audits `review.dismiss`, writes no hash/whitelist/ban/deletion), and `/queue` (MANAGE_GUILD, read-only) lists open oldest-first cards via the new `DetectionRepository.list_open` (excludes unposted and already-actioned rows, capped at 25 with jump links and coarse ages); no migration since `action_taken` is a plain `String(32)`; `/help` updated in both locales; +14 tests (#55, @rafs2006) <!-- pr-log:rafs2006/optimus#55 -->
- Setup-first fixes for `/setup`: `/config set` on scan-policy fields now invalidates `GuildConfigCache` post-commit; add nullable `detections.reported_at` (migration `0010_detection_reported_at`) so pre-`/setup` detections are replayed as review cards on first review-channel link (last 3 days, cap 50); defer join backfill until a review channel exists (fail-closed) and DM the guild owner a `/setup` prompt on join; `/config view` surfaces a pending-scan notice (#53, @rafs2006) <!-- pr-log:rafs2006/optimus#53 -->
- OCR lane now reads multi-panel scam screenshots: normalise all images toward `_MAX_OCR_DIM` (upscale as well as downscale, closing the 1000–1600px gap where collages OCR'd to noise) and raise `_OCR_TOTAL_TIMEOUT_SECONDS` from 3s to 8s (configurable as `detection_ocr_timeout_seconds`) so every preprocessing variant gets a slice of the budget (#52, @rafs2006) <!-- pr-log:rafs2006/optimus#52 -->
- Remove the self-serve `/forget_me` scanning opt-out (a single DM silently disabled scam detection in every server the bot is in) and replace it with a real GDPR-grounded `docs/privacy-policy.md`; keep the `users_optout` table/repo but drop the opt-out branch in `should_scan`; `MEMBER_COMMANDS` narrows to `{report, help}` (#51, @rafs2006) <!-- pr-log:rafs2006/optimus#51 -->
- Remove the `/appeal` command and the provably-uncalled `build_appeal_dm_row` / `build_appeal_review_row` builders (the flow wrote an `Appeal` row and told the member their appeal was sent, but no moderator was ever notified and the DM referenced a button that never rendered); drop the appeal-button sentence and five unrendered strings from `dm.warning`; README quickstart replaced with one authorize URL carrying the exact permission bits (`1099511720982`) including the previously-missing Manage Channels; `MEMBER_COMMANDS` self-derives to `{report, forget_me, help}` (#50, @rafs2006) <!-- pr-log:rafs2006/optimus#50 -->
